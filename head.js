// Returns first item of an array
const head = function(arrInput) {
  //takes the first index item of array and assigns to arrHead
  const arrHead = arrInput[0];
  //returns variable arrHead
  return arrHead;
};

module.exports = head;